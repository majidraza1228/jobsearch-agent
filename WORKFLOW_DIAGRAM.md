# Job Search Agent - Workflow Diagram

Visual guide showing how everything works together.

---

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         YOUR SETUP                               │
│                                                                  │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐     │
│  │   Indeed     │    │  Glassdoor   │    │   Monster    │     │
│  │   (RapidAPI) │    │  (RapidAPI)  │    │  (RapidAPI)  │     │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘     │
│         │                   │                    │              │
│         └───────────────────┼────────────────────┘              │
│                             │                                   │
│                             ▼                                   │
│                   ┌──────────────────┐                         │
│                   │  Job Search      │                         │
│                   │  Agent           │                         │
│                   │  (Python/Flask)  │                         │
│                   └────────┬─────────┘                         │
│                            │                                    │
│              ┌─────────────┼─────────────┐                    │
│              │             │             │                     │
│              ▼             ▼             ▼                     │
│       ┌───────────┐  ┌──────────┐  ┌─────────┐              │
│       │  SQLite   │  │   n8n    │  │ OpenAI  │              │
│       │  Database │  │ Webhooks │  │   API   │              │
│       │           │  │          │  │(Optional)│              │
│       └───────────┘  └──────────┘  └─────────┘              │
└─────────────────────────────────────────────────────────────────┘
```

---

## Part 1: Local Testing Flow

```
YOU (Terminal)
    │
    │ python -m src.main --search "Python Developer" --no-analyze
    │
    ▼
┌──────────────────────────────────────────────────┐
│  Job Search Agent (CLI)                         │
│  ─────────────────────────                      │
│  1. Read config.yaml                            │
│  2. Load enabled scrapers (Indeed, Glassdoor,  │
│     Monster - LinkedIn disabled)                │
│  3. Initialize database connection              │
└──────────────────┬───────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────┐
│  Scraper Orchestration                          │
│  ─────────────────────                          │
│  FOR each enabled platform:                     │
│    ├─ Indeed Scraper                            │
│    │   └─ API call to RapidAPI                  │
│    │       (https://indeed12.p.rapidapi.com)    │
│    │                                             │
│    ├─ Glassdoor Scraper                         │
│    │   └─ API call to RapidAPI                  │
│    │       (https://glassdoor-job-search...)    │
│    │                                             │
│    └─ Monster Scraper                           │
│        └─ API call to RapidAPI                  │
│            (https://monster-job-search...)      │
└──────────────────┬───────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────┐
│  Data Normalization                             │
│  ──────────────────                             │
│  Convert each job to standard format:           │
│  {                                               │
│    title: "Senior Python Developer"             │
│    company: "Tech Corp"                          │
│    location: "Remote"                            │
│    description: "Full text..."                  │
│    url: "https://indeed.com/job/123"            │
│    source: "indeed"                              │
│    posted_date: "2025-01-01"                    │
│  }                                               │
└──────────────────┬───────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────┐
│  AI Analysis (Optional)                         │
│  ─────────────────────────                      │
│  IF --no-analyze NOT specified:                 │
│    ├─ Send description to OpenAI                │
│    ├─ Extract:                                  │
│    │   • Required skills                        │
│    │   • Experience years                       │
│    │   • Education level                        │
│    │   • Summary                                │
│    └─ Add to job data                           │
│  ELSE:                                          │
│    └─ Skip (save money!)                        │
└──────────────────┬───────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────┐
│  Database Storage                               │
│  ────────────────                               │
│  1. Check if job exists (by external_id)       │
│  2. Skip if duplicate                           │
│  3. Insert new jobs                             │
│  4. Save search history                         │
└──────────────────┬───────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────┐
│  Display Results                                │
│  ───────────────                                │
│  Print to terminal:                             │
│  ─────────────────────────────────────────      │
│  Total jobs: 15                                 │
│  New jobs saved: 15                             │
│                                                  │
│  Platform breakdown:                            │
│    - indeed: 5                                  │
│    - glassdoor: 5                               │
│    - monster: 5                                 │
│                                                  │
│  1. Senior Python Developer at Tech Corp...     │
│  2. Python Engineer at StartupXYZ...            │
│  ...                                             │
└──────────────────────────────────────────────────┘
```

---

## Part 2: API Server Flow

```
YOU (Start Server)
    │
    │ python src/api/server.py
    │
    ▼
┌──────────────────────────────────────────────────┐
│  Flask API Server                               │
│  ────────────────                               │
│  ✓ Initialize database                         │
│  ✓ Load configuration                          │
│  ✓ Create job search agent                     │
│  ✓ Set up routes:                               │
│    • GET  /health                               │
│    • POST /api/search                           │
│    • GET  /api/jobs                             │
│    • GET  /api/jobs/{id}                        │
│    • POST /webhook/job-search (for n8n)        │
│    • POST /api/analyze                          │
│    • GET  /api/stats                            │
│  ✓ Start listening on port 5000                │
└──────────────────────────────────────────────────┘
         │
         │ Server running at http://localhost:5000
         │
         ▼
┌──────────────────────────────────────────────────┐
│  Waiting for requests...                        │
└──────────────────────────────────────────────────┘
```

---

## Part 3: n8n Integration Flow

```
┌──────────────────────────────────────────────────┐
│  n8n WORKFLOW                                   │
│  ────────────                                   │
│                                                  │
│  ┌────────────────┐                            │
│  │ 1. TRIGGER     │                            │
│  │  (Choose one)  │                            │
│  └────────┬───────┘                            │
│           │                                     │
│    ┌──────┴──────┬──────────────────┐         │
│    │             │                  │          │
│    ▼             ▼                  ▼          │
│  Webhook      Schedule          Manual         │
│  (HTTP POST)  (Cron: 9 AM)    (Click button)  │
│    │             │                  │          │
│    └─────────────┴──────────────────┘          │
│                  │                              │
│                  ▼                              │
│  ┌───────────────────────────┐                │
│  │ 2. SET PARAMETERS         │                │
│  │    (n8n Set Node)         │                │
│  │                           │                │
│  │  keywords: "Python Dev"   │                │
│  │  location: "Remote"       │                │
│  │  analyze: false           │                │
│  └────────────┬──────────────┘                │
│               │                                │
│               ▼                                │
│  ┌───────────────────────────┐                │
│  │ 3. HTTP REQUEST           │                │
│  │    Call Job Search API    │                │
│  │                           │                │
│  │  POST http://localhost:5000│               │
│  │       /webhook/job-search │                │
│  └────────────┬──────────────┘                │
│               │                                │
│               ▼                                │
└───────────────┼──────────────────────────────────┘
                │
                │ HTTP Request
                │
                ▼
┌──────────────────────────────────────────────────┐
│  Job Search Agent API                           │
│  ────────────────────────                       │
│  Receives:                                      │
│  {                                               │
│    keywords: "Python Developer",                │
│    location: "Remote",                          │
│    options: { analyze: false }                  │
│  }                                               │
│                                                  │
│  Executes search... (same as CLI flow above)   │
│                                                  │
│  Returns:                                       │
│  {                                               │
│    success: true,                               │
│    total_jobs: 15,                              │
│    new_jobs_saved: 15,                          │
│    platform_breakdown: {...},                   │
│    jobs: [...]                                  │
│  }                                               │
└──────────────────┬───────────────────────────────┘
                   │
                   │ JSON Response
                   │
                   ▼
┌──────────────────────────────────────────────────┐
│  n8n WORKFLOW (continued)                       │
│  ────────────────────────────                   │
│                                                  │
│  ┌────────────────────────────┐                │
│  │ 4. PROCESS RESPONSE        │                │
│  │    (n8n Function Node)     │                │
│  │                            │                │
│  │  - Extract job count       │                │
│  │  - Filter jobs by criteria│                │
│  │  - Format data             │                │
│  └──────────┬─────────────────┘                │
│             │                                   │
│    ┌────────┴────────┬──────────────┐         │
│    │                 │              │          │
│    ▼                 ▼              ▼          │
│  ┌──────┐      ┌──────────┐   ┌────────┐     │
│  │Email │      │  Slack   │   │ Save to│     │
│  │Alert │      │  Message │   │ Sheets │     │
│  └──────┘      └──────────┘   └────────┘     │
│                                                │
│  "Found 15 new jobs!"                         │
└──────────────────────────────────────────────────┘
```

---

## Part 4: Complete End-to-End Flow

```
┌─────────────────────────────────────────────────────────────┐
│  SCENARIO: Daily Automated Job Search                      │
└─────────────────────────────────────────────────────────────┘

9:00 AM - n8n Schedule Trigger Fires
    │
    ▼
n8n: Set search parameters
    • keywords: "Python Developer"
    • location: "Remote"
    │
    ▼
n8n: HTTP Request to localhost:5000/webhook/job-search
    │
    ▼
Job Search Agent receives request
    │
    ├─► Indeed Scraper
    │   └─► RapidAPI call
    │       └─► Returns 8 jobs
    │
    ├─► Glassdoor Scraper
    │   └─► RapidAPI call
    │       └─► Returns 5 jobs
    │
    └─► Monster Scraper
        └─► RapidAPI call
            └─► Returns 7 jobs
    │
    ▼
Total: 20 jobs found
    │
    ▼
Check database for duplicates
    • 15 are new
    • 5 already exist
    │
    ▼
Save 15 new jobs to SQLite database
    │
    ▼
Return response to n8n:
{
  "success": true,
  "total_jobs": 20,
  "new_jobs_saved": 15,
  "jobs": [...]
}
    │
    ▼
n8n: Process response
    │
    ├─► Filter: Remote only (12 jobs remain)
    │
    ├─► Send Email:
    │   "Good morning! Found 12 new remote Python jobs"
    │
    ├─► Post to Slack:
    │   "🎯 12 new remote Python Developer jobs!"
    │
    └─► Save to Google Sheets
        • Add rows with job details
        • Add "Applied" column
        • Add "Status" column
    │
    ▼
DONE! ✅

You receive:
  ✓ Email notification
  ✓ Slack message
  ✓ Updated spreadsheet
  ✓ Jobs saved in database
```

---

## Part 5: Cost Flow

```
┌─────────────────────────────────────────────────┐
│  COST BREAKDOWN (per search)                   │
└─────────────────────────────────────────────────┘

Search: "Python Developer" (Returns 20 jobs)

RapidAPI Costs:
  ├─ Indeed API call      : 1 request
  ├─ Glassdoor API call   : 1 request
  └─ Monster API call     : 1 request
  ────────────────────────────────────
  Total: 3 API requests

  Free tier: 100 requests/month = ~33 searches/month FREE
  Paid tier: $20/month = ~$0.60 per search (unlimited)

OpenAI Costs (if analyze: true):
  └─ 20 jobs × $0.01 = $0.20

  Without AI (analyze: false): $0.00

────────────────────────────────────────────

Example Monthly Costs:

Light Usage (2 searches/day, 60/month):
  • RapidAPI: FREE (under 100 requests)
  • OpenAI (no analyze): $0
  • Total: $0/month ✅

Medium Usage (5 searches/day, 150/month):
  • RapidAPI: $20/month (paid plan)
  • OpenAI (with analyze): ~$30/month
  • Total: $50/month

Cost Optimization:
  ✓ Use --no-analyze flag
  ✓ Use free RapidAPI tiers
  ✓ Schedule searches 1-2x daily max
  ✓ Filter jobs BEFORE AI analysis
```

---

## Quick Commands Reference

### Local Testing
```bash
# Search WITHOUT AI (FREE)
python -m src.main --search "Python Dev" --no-analyze

# Search WITH AI
python -m src.main --search "Python Dev"

# List jobs
python -m src.main --list

# Start API server
python src/api/server.py
```

### n8n Testing
```bash
# Start n8n
n8n start

# Test webhook
curl -X POST http://localhost:5678/webhook/job-search \
  -H "Content-Type: application/json" \
  -d '{"keywords": "Python Dev", "options": {"analyze": false}}'
```

### API Testing
```bash
# Health check
curl http://localhost:5000/health

# Search jobs
curl -X POST http://localhost:5000/api/search \
  -H "Content-Type: application/json" \
  -d '{"keywords": "Python Dev", "analyze": false}'

# Get jobs
curl http://localhost:5000/api/jobs?limit=10

# Stats
curl http://localhost:5000/api/stats
```

---

**See [GETTING_STARTED.md](docs/GETTING_STARTED.md) for complete step-by-step instructions!**
